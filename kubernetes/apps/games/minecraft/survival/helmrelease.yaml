---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: minecraft
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 5.1.1
  url: oci://ghcr.io/itzg/minecraft-server-charts/minecraft
---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app minecraft-survival
spec:
  chartRef:
    kind: OCIRepository
    name: minecraft
  dependsOn: []
  interval: 15m
  values:
    fullnameOverride: *app
    image:
      repository: ghcr.io/itzg/minecraft-server
      tag: 2026.3.0@sha256:96d4812e29e2e820831f86a089002f7e78665f830cb4f16b23d5880d48cf90a7
    resources:
      requests:
        cpu: 1000m
      limits:
        memory: 8Gi
    securityContext:
      runAsUser: 1000
      fsGroup: 100
    livenessProbe:
      initialDelaySeconds: 30
    readinessProbe:
      initialDelaySeconds: 30
    startupProbe:
      enabled: true
    extraEnv:
      TZ: America/Chicago
    persistence:
      storageClass: longhorn
      dataDir:
        enabled: true
        Size: 10Gi
    # serviceAnnotations:
    #   mc-router.itzg.me/externalServerName: mc.phekno.io
    minecraftServer:
      eula: true
      # version: 1.21.1
      # type: FORGE
      overrideServerProperties: true
      jvmXXOpts: "-XX:MaxRAMPercentage=75"
      difficulty: normal
      spawnProtection: 0
      gameMode: survival
      pvp: false
      # onlineMode: https://kube-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
      ops: "phekno,quinnyf13"
      whitelist: "phekno,quinnyf13,harpyf13,LivParrot,abbykinz2013"
      motd: "Phekno's Survival Server"
      worldSaveName: phekno-survival
      viewDistance: 12
      modUrls: []
      rcon:
        enabled: true
      serviceType: LoadBalancer
      serviceAnnotations:
        io.cilium/lb-ipam-ips: 10.100.50.156
  valuesFrom:
    - kind: Secret
      name: *app
      valuesKey: RCON_PASSWORD
      targetPath: minecraftServer.rcon.password

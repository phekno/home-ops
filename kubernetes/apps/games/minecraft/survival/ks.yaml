---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app minecraft-survival
  namespace: &namespace games
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  targetNamespace: *namespace
  components:
    # - ../../../../components/volsync
  dependsOn:
    - name: onepassword-store
      namespace: external-secrets
    # - name: longhorn
    #   namespace: longhorn-system
  path: ./kubernetes/apps/games/minecraft/survival
  postBuild:
    substitute:
      APP: *app
      GATUS_TCP_GROUP: minecraft
      # playit reassigns the port whenever a tunnel's region changes or it is
      # recreated -- all three moved when they were switched to chicago-illinois.
      # The SRV record _minecraft._tcp.phekno.playit.plus is the source of
      # truth; players follow it automatically, this check does not.
      GATUS_TCP_ENDPOINT: phekno.playit.plus:30973
      VOLSYNC_CAPACITY: 1Gi
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m
